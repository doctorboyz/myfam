# === VPS / Docker ===
# Used by docker-compose.yml
POSTGRES_PASSWORD=change_me_strong_password

# Used inside the app container (auto-configured via docker-compose)
DATABASE_URL=postgresql://myfam:change_me_strong_password@db:5432/myfam

# === Local development ===
# DATABASE_URL="postgresql://root:rootpassword@localhost:5432/myfamdb"
